#include <stdio.h>

/*
* Entry point
*
*Return: Always 0 (Success)
*/

int main(void)
{
	/* prints to stdout using printf */
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
